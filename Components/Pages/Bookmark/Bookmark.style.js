'use strict';
import { StyleSheet } from 'react-native';

module.exports = StyleSheet.create({
    cardStyle: {
        borderWidth: 1,
        marginBottom: 10,
        borderRadius: 5,
        borderColor: 'black',
    },
});
